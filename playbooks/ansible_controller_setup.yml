- name: Setup Ansible Controller
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Install required packages
      become: true
      apt:
        name:
          - ansible-core
          - bash-completion
        state: present
        update-cache: true

    - name: Add Ansible bash completion
      blockinfile:
        path: /etc/bash.bashrc
        block: |
          source /usr/share/bash-completion/completions/ansible

