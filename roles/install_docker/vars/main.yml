# NOTE: Docker's apt repository uses its own architecture names (amd64/arm64), which differ
#       from Ansible's (x86_64/aarch64).
install_docker_apt_arch: "{{ {'x86_64': 'amd64', 'aarch64': 'arm64'}.get(ansible_facts.architecture, ansible_facts.architecture) }}"
